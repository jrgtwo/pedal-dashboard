export type SandboxProps = null

export type RequiredDataValues = {
  id?: number,
  dragId: number | string,
  w: number,
  h: number,
  x: number,
  y: number,
  rotation: number,
  created_at?: string,
}
